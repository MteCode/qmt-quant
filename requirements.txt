# ---- 核心依赖 ----
pandas>=2.0,<3.0
numpy>=1.24,<3.0
pyarrow>=14.0          # Parquet 落地
PyYAML>=6.0            # 配置加载

# ---- 外部数据源 ----
tushare>=1.4            # 历史指数成分股、逐日估值因子、行业分类、全收益指数
                        # 需 2000 积分（200 元/年），token 走环境变量 TUSHARE_TOKEN

# ---- 研究（可选，不装不影响回测与实盘）----
pyqlib>=0.9.7           # 微软 Qlib：ML 模型库 + Alpha158/360 因子集
                        # ⚠ 它会把 pandas 锁到 <3.0（实测从 3.0.5 降到 2.3.3）
scipy>=1.11             # Qlib 的 spearman 相关需要；本项目的 IC 分析也用

# ---- 调度与通知 ----
APScheduler>=3.10       # 盘前/盘中/盘后定时任务
requests>=2.31          # 企微/钉钉 webhook

# ---- 报告与可视化 ----
matplotlib>=3.7
plotly>=5.18            # 交互式 HTML 回测报告

# ---- 开发与测试 ----
pytest>=7.4
pytest-cov>=4.1

# ---- 券商 SDK（不通过 pip 安装）----
# xtquant: 从 miniQMT / 大 QMT 客户端安装目录复制到 site-packages，
#          或把 <QMT安装目录>\bin.x64\Lib\site-packages 加入 PYTHONPATH。
#          版本必须与本机 QMT 客户端一致，pip 上的第三方同名包不要用。
