# 任务表

> 状态：`TODO` 未开始 / `DOING` 进行中 / `DONE` 已完成 / `BLOCKED` 被阻塞
> 更新日期：2026-08-26

## 当前进度总览

| 阶段 | 任务 | 状态 |
|------|------|------|
| 已完成 | 框架内核、回测引擎、风控、miniQMT 网关 | DONE |
| 已完成 | 沪深300 行情（日/周/1分钟）+ 财务数据 | DONE |
| 已完成 | 标的池与偏差处理、趋势策略、选股策略 | DONE |
| **本轮** | T1 数据清洗 | **DONE** |
| **本轮** | T1.5 全面检修 | **DONE** |
| **本轮** | **T2 可视化报告 → T3 回撤控制** | DOING |

---

## T1.5 全面检修（2026-08-26）

对全部代码与数据做一次系统性检查，发现并修复 4 个问题。

| # | 发现 | 严重性 | 处理 |
|---|------|--------|------|
| 1 | **分钟线仍是前复权** | 高 | 只重下了日线周线，1m 遗漏。同日同股日线 32.47 / 分钟线 16.99，跨周期策略必错。已重下 300 只，三周期口径现已一致 |
| 2 | **`MiniQmtGateway` 未实现 `query_orders`** | 高 | 一直用基类空实现，`reconcile()` 永远报「活动委托 0 笔」。实盘重启看不到已挂单，可能重复下单。已实现并验证能识别出账上那笔挂单 |
| 3 | 代码默认复权仍是 `front` | 中 | 配置改了但构造函数默认没改，不传参就会拿到会产生负价格的数据。已改为 `back` |
| 4 | `live_engine.py` 引用未导入的 `BarData` | 低 | 局部变量注解运行时不求值故未崩溃，属潜在雷。已修 |

另清理：未使用导入 9 处、硬编码限价缓冲 2 处（改为 `price_buffer` 参数）。

### 检修覆盖项

| 项目 | 结果 |
|------|------|
| 单元测试 | 169 passed |
| pyflakes 静态检查 | 干净 |
| 脚本冒烟测试 | 13/13 可启动 |
| 回测不变量（净值/现金/持仓/整手/费用/T+1/净值恒等） | 7/7 通过 |
| 风控拦截路径 | 11/11 正确 |
| 跨周期复权一致性 | 4/4 一致 |
| 全量负价格复查 | 900 文件 0 例 |
| 实盘链路 dry-run | 连接/对账/预热/订阅/启动全通 |
| 文档引用文件存在性 | 全部存在 |

---

## T1 数据清洗模块（P0）

**为什么最先做**：脏数据会让后面所有结论失效，且和已踩的两个坑同类 ——
不报错，只是悄悄给出错的结果。

| # | 子任务 | 说明 | 状态 |
|---|--------|------|------|
| 1.1 | OHLC 逻辑校验 | `high >= low`、`close/open ∈ [low, high]`、价格 > 0 | DONE |
| 1.2 | 异常涨跌幅检测 | 按板块区分涨跌停幅度（主板10%/创业板科创板20%/北交所30%） | DONE |
| 1.3 | 缺失交易日检测 | 对照交易日历找缺口，长期停牌归为可疑而非错误 | DONE |
| 1.4 | 复权跳变检测 | 由涨跌幅异常规则覆盖 | DONE |
| 1.5 | 零成交量矛盾 | 成交量为 0 但价格有变动 | DONE |
| 1.6 | 财务数据校验 | 公告日早于报告期、公告日在未来、追溯重述识别 | DONE |
| 1.7 | 清洗报告与 CLI | `scripts/check_data.py`，分级输出并导出 CSV | DONE |

**状态：DONE**（2026-08-26）

### 实际发现的问题

| 发现 | 严重性 | 处理 |
|------|--------|------|
| **前复权产生负价格** | 致命 | 7 只共 851 根 K 线为负（601919 最低 -5.14）。高分红股前复权倒推会减出负值。**已改用后复权** |
| **`get_asof` 取到旧报告期的重述版** | 致命 | 财报追溯重述导致同报告期多条记录。原按公告日排序，`get_asof('2009-04-01')` 返回 2007 年重述（EPS 0.97）而非 2008 年报（EPS 0.20）。**已改为按 (报告期, 公告日) 排序** |
| Capital 表公告日早于报告期 | 误报 | 其「报告期」实为股本变更生效日，先公告后生效正常。**规则已限定为定期报告** |
| 688506 零成交量但报价变动 | 无影响 | 7/58675 根，科创板极低流动性分钟。已标记停牌被引擎跳过 |
| 涨跌幅异常 1418 条 | 待观察 | 237 只涉及，多为停牌复牌首日与真实极端行情，非数据错误 |

### 验收结果

```
沪深300 × (日线/周线/1分钟线) + 财务 5 表
错误 1 条（已确认无影响） | 可疑 241 条 | 提示 21155 条（财报重述，正常）
```

单测 165 passed，其中校验器与 point-in-time 取数 28 个。

---

## T2 回测可视化报告（P0）

**为什么做**：现在只有 CSV，得用 Excel 翻。看图 10 秒能发现的问题，
翻 CSV 半小时也未必看得出来。`requirements.txt` 里的 matplotlib/plotly
目前一行没用。

| # | 子任务 | 说明 | 状态 |
|---|--------|------|------|
| 2.1 | 净值曲线 | 策略净值（基准接口已备好，待下载指数数据） | DONE |
| 2.2 | 回撤曲线 | 水下图，标注最大回撤区间 | DONE |
| 2.3 | 交易点标注 | 单标的回测在价格图上标买卖点 | DONE |
| 2.4 | 月度收益热力图 | 年 × 月，正红负绿 | DONE |
| 2.5 | 指标汇总表 | 绩效指标 + 偏差说明 + 执行诊断 | DONE |
| 2.6 | HTML 报告生成 | 单文件自包含（plotly.js 内联 4.1MB） | DONE |
| 2.7 | 多标的图表 | 持仓只数变化、分标的盈亏排行 | DONE |
| 2.8 | 执行诊断 | 不足一手告警、拒单原因统计 | DONE |

**状态：DONE**（2026-08-27）

### 过程中发现的严重问题：后复权抬高价格导致标的买不进

后复权价远高于真实价（茅台 8152 vs 实际约 1300），一手的**名义成本**
被放大数倍，百股取整的粒度因此变粗。

实测（100 万本金 / 10 只持仓，每只预算 9.5 万）：
- **36 只（12%）取整后为 0 股，完全买不进**（000651、600519、300502…）
- 另有 72 只闲置资金 >10%，最坏闲置 49.8%

更糟的是它**静默发生**：策略原本在 `rebalance()` 里自行取整，
不足一手就 `continue` 跳过，引擎完全不知情，报告里也看不出标的被排除。

已做的处理：
1. 取整逻辑收敛到引擎一处，策略不再自行取整
2. 引擎统计 `undersized_orders`，报告中以醒目告警呈现
   （实测组合回测触发 28 只标的受影响）
3. `lot_size` 改为可配置，研究阶段可设 1 消除该假象

**根本解法尚未实现**：应按真实价取整，即在后复权空间中用
`lot_size / F(t)` 作为最小单位，其中 `F(t) = 后复权价 / 真实价`。
需要下载复权因子（`xtdata.get_divid_factors`）或同时保存不复权收盘价。

---

## T3 回撤控制（P0）

**为什么做**：现在风控只有「当日亏损线」，防不住连续阴跌。
连跌 20 天每天 1%，累计亏 20%，一次都不会触发日亏线。

| # | 子任务 | 说明 | 状态 |
|---|--------|------|------|
| 3.1 | 峰值回撤跟踪 | 记录账户净值峰值，实时计算当前回撤 | TODO |
| 3.2 | 分档响应 | 一档只平不开、二档强制减仓、三档全平急停 | TODO |
| 3.3 | 恢复条件 | 回撤收复到什么程度才解除限制，防止反复触发 | TODO |
| 3.4 | 实盘引擎接入 | 接到 LiveEngine，并在盘后报告中体现 | TODO |
| 3.5 | 回测中生效 | 回测也走同一套回撤控制，保证回测实盘一致 | TODO |

**验收标准**：构造连续阴跌场景，验证各档位按预期触发与恢复。

---

## 待办（下一轮）

| # | 任务 | 说明 | 状态 |
|---|------|------|------|
| T4 | 基准对比 | 下载沪深300 指数，回测统一对标 | TODO |
| T5 | 参数寻优 + 样本外验证 | 网格寻优、walk-forward、参数平原检查 | TODO |
| T6 | 盘后日报与告警 | 每日持仓盈亏报告 + 企微/钉钉推送 | TODO |
| T7 | 财务因子选股 | 用已下载的 ROE/成长/估值做多因子选股 | TODO |
| T8 | 退市股行情补齐 | 等东财限流解除后重跑 `download_delisted.py --resume` | BLOCKED |

---

## 遗留事项（需人工确认）

| # | 事项 | 说明 | 状态 |
|---|------|------|------|
| L1 | 撤销测试挂单 | 模拟账户 62221162 有一笔 100 股 @10.67（1067 元）买单，撤单指令已提交但收盘时段不生效，**次日开盘需确认已撤销** | BLOCKED（等开盘） |
| L2 | 实盘下单验证 | 交易时段跑 `scripts/test_order_flow.py` 验证完整报单/回报/撤单链路 | BLOCKED（等开盘） |
| L3 | 涨跌停幅度按板块区分 | 已实现：`core/constants.get_price_limit()` 按代码前缀判定，回测引擎与校验器共用。实测影响 0.225%（171/76160 交易日） | DONE |

---

## 已知未解决问题（截至 2026-08-26）

### 架构文档承诺但未实现的模块

| 文件 | 影响 | 优先级 |
|------|------|--------|
| `store/database.py` | **策略状态无持久化**，进程崩溃重启后策略持仓、指标窗口全部丢失 | 高（实盘前必须补） |
| `utils/notifier.py` | 无告警推送，风控拦截与断线只写日志，无人实时可见 | 中（T6） |
| `utils/calendar.py` | 无交易日历，`check_data.py` 的缺失交易日检测因此未启用 | 中 |
| `gateway/amt_gateway.py` | AMT 通道未接入 | 低（原计划 v0.5） |

### 数据层遗留

| 问题 | 说明 | 状态 |
|------|------|------|
| 幸存者偏差**实际仍未消除** | `FullMarketUniverse` 代码已就绪，但 361 只退市股的行情未下载（东财限流） | BLOCKED |
| 涨跌幅异常 1418 条 | 237 只涉及，抽查多为停牌复牌首日与真实极端行情，未逐一核实 | 待观察 |
| 公告日在未来 2 条 | 未查证 | 待观察 |
| 688506 零成交量报价变动 7 根 | 科创板极低流动性分钟，占 0.012%，已标记停牌被引擎跳过 | 无影响 |
| 分钟线仅 1 年 | 券商侧限制，非缺陷 | 约束 |
