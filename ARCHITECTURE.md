# 系统架构

一个人操作、50 万本金、A 股日频、回撤硬约束 20% —— 所有设计取舍都从这四条出发。
不追求通用框架，只追求「每个环节的错误都能被发现」。

## 全局数据流

```
                            ┌─────────────────────────────┐
  QMT / Tushare / AkShare ──▶  1 数据层                    │
                            │    下载 → 清洗 → 存储 → 校验 │
                            └──────────────┬──────────────┘
                                           │  data/clean/  唯一真相源
                        ┌──────────────────┼──────────────────┐
                        ▼                  ▼                  ▼
              ┌──────────────┐   ┌──────────────┐   ┌──────────────┐
              │ 2 因子层     │   │ 3 训练层     │   │ 8 展示层     │
              │  挖掘/评估   │──▶│  模型/集成   │   │  管理台      │
              └──────┬───────┘   └──────┬───────┘   └──────▲───────┘
                     └──────────┬───────┘                  │
                                ▼                          │
                     ┌──────────────────┐                  │
                     │ 4 回测层         │──────────────────┤
                     │  撮合/成本/风控  │                  │
                     └────────┬─────────┘                  │
                              ▼                            │
                     ┌──────────────────┐                  │
                     │ 5 信号层         │                  │
                     │  选股 + 择时     │                  │
                     └────────┬─────────┘                  │
                              ▼                            │
                     ┌──────────────────┐                  │
                     │ 6 风控层（贯穿） │                  │
                     └────────┬─────────┘                  │
                              ▼                            │
                     ┌──────────────────┐                  │
                     │ 7 交易层         │──────────────────┘
                     │  模拟盘/实盘     │
                     └──────────────────┘
```

---

## 1 数据层

### 已有

| 功能 | 实现 | 说明 |
|------|------|------|
| 行情下载 | `scripts/download_data.py` | QMT 日线/分钟线，支持断点续传 |
| 指数与成分 | `download_index_weight.py` `build_history_universe.py` | 历史成分，消除幸存者与成分股前视 |
| 财务数据 | `download_financial.py` | 四表 + 每股指标 |
| 资金流 | `download_money_flow.py` `build_money_factors.py` | 龙虎榜、两融、北向 |
| 退市名录 | `download_delisted.py` | 幸存者偏差的关键 |
| **数据清洗** | `qmtquant/datafeed/cleaner.py` `scripts/clean_data.py` | 单一清洗层，见下 |
| 数据校验 | `qmtquant/datafeed/validator.py` `scripts/check_data.py` | 只报告不修改 |
| Qlib 导出 | `qlib_export.py` | 训练数据格式 |
| 关系库 | `scripts/build_database.py` | SQLite，供 SQL 查询与核对 |

### 清洗层设计

```
data/1d/          原始下载，不可变，可回溯
      │  scripts/clean_data.py   ← 唯一清洗出口
data/clean/1d/    清洗层，唯一真相源
      ├──> data/qlib_data/   训练与回测（bin，快 25 倍）
      └──> data/market.db     查询与核对（SQL）
```

**只修有唯一正确答案的**：重复时间戳保留最后一条、时间排序、负成交量置零、
非正价格整行剔除（不填补 —— 填补等于凭空造价格）。

**需要猜测的只标记不改**：OHLC 矛盾、涨跌幅异动、缺失交易日。

保留原始层是有意的：清洗规则会演进，规则一改要能重新清洗。

### 缺失

| 缺什么 | 影响 | 优先级 |
|--------|------|--------|
| **融资融券数据不全** | 2023 年后每年仅 24 天（覆盖 10%），因子被迫剔除 | 高 |
| **财务数据仅覆盖 7%** | 3185 个文件只对应 206 只成分股，做不了基本面因子 | 高 |
| 复权因子独立存储 | 现在只有后复权价，无法还原原始价与复权因子 | 中 |
| 停牌/ST 状态历史 | 回测无法准确模拟停牌不可交易、ST 涨跌停 5% | 中 |
| 分钟线覆盖 | 仅 300 只，做不了日内策略 | 低 |

---

## 2 因子层

### 已有

| 功能 | 实现 |
|------|------|
| 因子存储 | `datafeed/factor_store.py` |
| IC 分析 | `research/factor_ic.py` `scripts/analyze_factors.py` |
| 因子挖掘 | `scripts/mine_factors.py` |
| **候选因子评估** | `strategies/*/eval_factors.py` |
| **多因子合成** | `strategies/*/combine_factors.py` |

### 评估框架的三道关

一个因子要同时通过才可用：

1. **有预测力** —— IC 显著。用 **Newey-West** 修正重叠窗口造成的 t 值虚高
   （未修正 t=-24.44，修正后 -7.22）
2. **跨期稳定** —— 分段 IC 不翻转
3. **与现有信号正交** —— 与 ALSTM 相关性低。相关性高意味着信息重复

另有两道硬门槛：**可获得滞后**（防前视，龙虎榜滞后 1 日、两融 2 日）、
**数据完整度**（有效截面须占交易日 60% 以上）。

### 缺失

| 缺什么 | 影响 | 优先级 |
|--------|------|--------|
| **行业中性化** | 因子暴露集中在少数行业，赚的是行业 beta 而非 alpha | 高 |
| **市值中性化** | 小市值效应会污染几乎所有因子 | 高 |
| 因子正交化 | 多因子间共线，权重不稳定 | 中 |
| 分层回测 | 只看 IC 不看分层单调性，无法判断因子是否线性有效 | 中 |
| 因子衰减分析 | 不知道信号有效期多长，调仓周期靠猜 | 中 |
| 因子库统一管理 | 因子散落各处，无版本与血缘 | 低 |

---

## 3 训练层

### 已有

| 功能 | 实现 |
|------|------|
| ALSTM 训练 | `strategies/*/train_alstm.py`，Alpha360 + Qlib 模型库 |
| PPO 择时 | `strategies/*/train_ppo.py`，stable-baselines3 |
| **权重持久化** | `alstm_weights.pt` + `alstm_meta.json`，可复现推理 |
| **多种子实验** | `seed_experiment.py`，量化训练随机性 |
| **集成** | `train_ensemble.py`，截面排名平均 |
| **集成规模分析** | `ensemble_scaling.py`，测量方差收敛 |

### 已建立的方法论

这套流程的价值在于**能快速判定一个策略有没有 alpha**：

```
多种子 → 看 Sharpe 分布是否横跨零轴
集成   → 看方差是否随规模收敛（不收敛=模型高度相关）
参数扫描 → 看是否孤立尖刺（尖刺=参数挖掘）
分段检验 → 看前后两段符号是否一致
```

四项两小时内跑完。已用它否定了 ALSTM 单模型（Sharpe 中位数 -0.027）。

### 缺失

| 缺什么 | 影响 | 优先级 |
|--------|------|--------|
| **PPO 多种子实验** | PPO 自身随机性从未度量，当前 Sharpe 0.899 可能是右尾抽样 | 高 |
| **滚动窗口训练** | 现在是固定切分，实盘应定期用新数据重训 | 高 |
| 超参搜索 | 全靠默认值，没系统调过 | 中 |
| 异构集成 | 同架构模型高度相关（实测方差比理论差 2.1 倍） | 中 |
| 训练实验追踪 | 有 mlruns 但没用起来 | 低 |

---

## 4 回测层

### 已有

| 功能 | 实现 |
|------|------|
| 撮合引擎 | `engine/backtest_engine.py`，T+1、涨跌停、整手 |
| 成本模型 | `config.CostConfig`，佣金/印花税/过户费/滑点 |
| 绩效计算 | `engine/performance.py` |
| 标的池 | `universe/providers.py`，历史成分 + 上市日过滤 |
| 回撤控制 | `risk/drawdown.py`，三档 + 迟滞 + 冻结期 |
| 稳健性验证 | `research/validation.py`，参数邻域检查 |
| HTML 报告 | `report/html_report.py` |

### 缺失

> **更正**：先前版本把「整手约束未模拟」列为第一优先缺口，这是错的。
> `backtest_engine.py` 已实现整手取整（`volume // lot_size * lot_size`），
> 并统计买不满一手的标的（`undersized_orders`）而非静默丢弃。

| 缺什么 | 影响 | 优先级 |
|--------|------|--------|
| **后复权价导致整手成本虚高** | 一手名义成本被抬高（茅台约 6 倍），实测 100 万/10 只时沪深300 有 36 只完全买不进。根本解法是按真实价取整，需复权因子 | 高 |
| **停牌不可交易** | 回测假设总能成交 | 高 |
| 冲击成本 | 只有固定滑点，未按成交量占比建模 | 中 |
| 归因分析 | 不知道收益来自行业、风格还是选股 | 中 |
| 多策略组合回测 | 只能单策略 | 低 |
| 走查回测 | 没有 walk-forward | 中 |

---

## 5 信号层

### 已有

| 功能 | 实现 |
|------|------|
| 选股 | `strategy/signal_rank.py`，按分数取 Top-K |
| 择时 | PPO 输出仓位水平 |
| 信号生成 | `strategies/*/generate_signal.py` |
| 组合构建 | `strategy/portfolio.py` |
| 逐仓风控 | `strategy/trade_manager.py`，止损/止盈/移动止损 |

### 缺失

| 缺什么 | 影响 | 优先级 |
|--------|------|--------|
| **权重优化** | 只有等权，无风险平价/最小方差 | 中 |
| **换手率约束** | 无换手上限，成本被动承受（实测 4.7 年 2385 笔、手续费 2 万） | 高 |
| 个股权重上限 | 组合可能过度集中 | 中 |
| 信号衰减处理 | 调仓日之外信号不更新 | 低 |

---

## 6 风控层（贯穿）

### 已有

| 层级 | 实现 | 触发 |
|------|------|------|
| 下单前置 | `risk/risk_manager.py` | 单笔金额、单票占比、总仓位、当日笔数、黑名单 |
| 日内 | 日亏损线 3% | 只平不开 |
| 跨日 | `risk/drawdown.py` 三档 | 6% 停开仓 / 9% 减仓至 30% / 12% 清仓 |
| 盘中监控 | `strategies/*/risk_monitor.py` | 常驻进程，状态持久化 |
| 全局急停 | kill_switch | 拒绝一切下单 |

**状态跨进程共享**：`state/risk_state.json`，盘中监控与下单脚本读同一份 ——
否则会出现「监控说只平不开、下单脚本照样开新仓」的分裂。

### 缺失

| 缺什么 | 影响 | 优先级 |
|--------|------|--------|
| **训练时未纳入风控** | PPO 训练环境无回撤硬约束，学到的行为与实盘执行不一致 | 中 |
| 行业/风格暴露限制 | 无行业集中度上限 | 中 |
| 流动性风控 | 未检查成交量能否容纳委托 | 中 |
| 异常成交告警 | 成交价偏离预期无提醒 | 低 |

---

## 7 交易层

### 已有

| 功能 | 实现 |
|------|------|
| 网关抽象 | `gateway/base.py` |
| miniQMT | `gateway/xt_gateway.py` |
| 模拟网关 | `gateway/sim_gateway.py` |
| 实盘引擎 | `engine/live_engine.py` |
| 下单执行 | `strategies/*/paper_trade.py`，先卖后买，逐笔过风控 |
| 持仓快照 | `strategies/*/snapshot_positions.py` |
| 执行记录 | `strategies/*/executions/` |
| 定时调度 | `webui/scheduler.py`，盘前/盘后 |

### 缺失

| 缺什么 | 影响 | 优先级 |
|--------|------|--------|
| **成交回报对账** | 下单后不核对实际成交，不知道有没有废单 | 高 |
| **滑点统计** | 不知道实际成交价与信号价差多少 | 高 |
| 拆单算法 | 大单一次性市价，冲击成本高 | 中 |
| 撤单重试 | 未成交订单不处理 | 中 |
| 多账户 | 只支持单账户 | 低 |

---

## 8 展示层

### 已有

| 页面 | 内容 |
|------|------|
| 总览 | 净值/回撤曲线、模型产物、风控档位、关键指标 |
| 策略选股 | 回测各调仓日买卖明细、行业分布 |
| 实盘执行 | 账户实际持仓、委托记录、与信号差异 |
| 数据核对 | 二进制数据可视化、自动查错、一致性扫描、CSV 导出 |
| 实验结果 | 多种子方差、集成收敛、参数网格、跨期稳定性 |
| 运行任务 | 白名单任务一键触发 |
| 定时任务 | 盘前/盘后调度配置 |
| 运行记录 | 历史任务与实时日志 |

**安全约束**：只监听 127.0.0.1；可执行动作走白名单；下单类任务二次确认。

### 缺失

| 缺什么 | 影响 | 优先级 |
|--------|------|--------|
| **清洗报告页** | 清洗结果只能看日志 | 中 |
| **实盘净值曲线** | 只有回测曲线，没有实盘每日净值 | 高 |
| 归因图表 | 无收益来源拆解 | 中 |
| 告警通知 | 风控触发、任务失败无推送 | 中 |
| 因子库浏览 | 因子 IC、相关性无可视化 | 低 |

---

## 优先级汇总

按「不做会出错」而非「做了更好」排序：

### 第一优先：正确性

1. **PPO 多种子实验** —— 已有直接证据表明它不稳定：同一套代码，
   仅选股分数从 ALSTM 换成 LightGBM，学出的平均仓位从 30% 跳到 70%，
   回撤从 13.96% 恶化到 33.29%。之前那个 Sharpe 0.899 的可信度存疑
2. **成交回报对账 + 滑点统计** —— 实盘跑起来后无法评估执行质量
3. **停牌不可交易** —— 回测高估了可成交性
4. **实盘净值曲线** —— 无法判断实盘是否符合回测预期
5. **按真实价取整** —— 后复权价把一手成本抬高数倍，造成虚假的资金闲置

### 第二优先：结论可信度
6. **行业/市值中性化** —— 因子可能只是行业 beta
7. **滚动窗口训练** —— 固定切分与实盘不符

### 第三优先：能力扩展

8. **补齐两融与财务数据** —— 打开基本面因子这条线
9. **换手率约束** —— 成本是已知的收益杀手
10. **分层回测与因子衰减** —— 判断因子有效性的标准工具

---

## 已建立的方法论

这些是踩坑换来的，比任何单个功能都重要：

| 教训 | 由来 |
|------|------|
| 单次回测数字不可信 | 8 种子实验 Sharpe 区间 -0.491 ~ +0.532，中位数 -0.027 |
| 参数网格看邻域，不看最优点 | 50只/20日 Sharpe +0.650，邻居 -1.080，孤立尖刺 |
| 必须做分段检验 | 全期冠军在两个子段都是负的 |
| IC 显著 ≠ 组合赚钱 | IC 与 Sharpe 相关性仅 0.338 |
| 重叠窗口的 t 值要修正 | 未修正 -24.44，Newey-West 后 -7.22 |
| 因子必须按可获得时点滞后 | margin_bal_chg 滞后 1 日即失去显著性 |
| 数据完整度是硬门槛 | 两融因子仅 115/1108 期有数据，IC 不可信 |
| 集成能压回撤，不能造 alpha | 回撤达标率 50% → 100%，Sharpe 中位数仍 0.08 |
